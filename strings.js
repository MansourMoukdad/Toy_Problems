



// Write a function called characPosit, when provided with a 
//letter, return its position in the alphabet.
// 	Input :: "a"
// 	Ouput :: "Position of alphabet: 1"
	function characPosit(character){   // 
		var character=["a","b","c"]
		var result="";
		var posit=length[i]

		while(i<character.){

		}
		return "Position of alphabet:" + character.length;
	}


// Write a function called repeatStr which repeats the given
// string string exactly n times.
// repeatStr("hello",4) ==> 'hellohellohellohello'
	
	function repeatStr (n, s) {
		var result ="";

		var counter = 1;

		for(var i = 0; i<n.length; i++ ){

			result = result * n.length;

			counter = counter +1;
		}
		return result;
	}